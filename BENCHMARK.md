# tre-mem — A/B Precision Benchmark

> Generated by `node scripts/benchmark.mjs`

## Configuration

- Project: `tre-mem`
- Target branch: `main`
- Top-K: 10
- Queries: 10
- Branch-tagged observations on `main`: **22** (distinct branches in project: 1)

## Methodology

- **Baseline (A)** — `Fts5SemanticSearcher` over `~/.claude-mem/claude-mem.db` (`observations_fts`, BM25 ranked). This is the closest in-process proxy for raw `claude-mem.search` since claude-mem itself indexes via the same FTS5 table.
- **tre-mem (B)** — `searchBranchContext` (3-signal weighted rerank: semantic + branch + recency, plus pin boost).
- For each query we record the top-K ids returned by A and B, then compute:
  - `precision@K_branch` — fraction of top-K observations whose `branch_tag.branch` equals the target branch.
  - `jaccard(A,B)` — overlap of the two top-K id sets.
  - B-top-1 score breakdown (`semantic / branch / recency / pin`) to show where rank came from.

## Per-query results

| Query | A hits | B hits | A precision@K | B precision@K | Jaccard | B top-1 total | B top-1 breakdown |
|---|---:|---:|---:|---:|---:|---:|---|
| `FTS5 BM25 semantic searcher` | 10 | 10 | 0.000 | 1.000 | 0.000 | 0.419 | sem 0.02 / br 0.40 / rec 0.00 / pin 0.00 |
| `git reflog backfill branch` | 10 | 10 | 0.300 | 1.000 | 0.176 | 0.623 | sem 0.22 / br 0.40 / rec 0.00 / pin 0.00 |
| `MCP server stdio tool` | 10 | 10 | 0.200 | 0.900 | 0.176 | 0.600 | sem 0.40 / br 0.00 / rec 0.20 / pin 0.00 |
| `weighted rerank signal` | 10 | 10 | 0.100 | 1.000 | 0.053 | 0.441 | sem 0.04 / br 0.40 / rec 0.00 / pin 0.00 |
| `session-start hook claude code` | 10 | 10 | 0.600 | 1.000 | 0.429 | 0.800 | sem 0.40 / br 0.40 / rec 0.00 / pin 0.00 |
| `pin graduate fact` | 10 | 10 | 0.000 | 0.900 | 0.053 | 0.599 | sem 0.40 / br 0.00 / rec 0.20 / pin 0.00 |
| `claude-mem adapter readonly` | 10 | 10 | 0.200 | 1.000 | 0.111 | 0.519 | sem 0.12 / br 0.40 / rec 0.00 / pin 0.00 |
| `branch_tag schema migration` | 10 | 10 | 0.300 | 1.000 | 0.176 | 0.470 | sem 0.07 / br 0.40 / rec 0.00 / pin 0.00 |
| `epoch normalization milliseconds` | 10 | 10 | 0.100 | 0.900 | 0.111 | 0.599 | sem 0.40 / br 0.00 / rec 0.20 / pin 0.00 |
| `test fixture vitest` | 10 | 10 | 0.100 | 1.000 | 0.053 | 0.444 | sem 0.04 / br 0.40 / rec 0.00 / pin 0.00 |

## Aggregate

- Mean A precision@10 = **0.190**
- Mean B precision@10 = **0.970**
- Mean Jaccard(A, B) = **0.134**

## Interpretation

> Precision delta (B − A) reflects how much the 3-signal rerank concentrates branch-tagged observations at the top of results versus a pure semantic baseline. Higher B precision means tre-mem successfully surfaces observations that are known to belong to the working branch (and, in a multi-branch dataset, suppresses off-branch noise). Jaccard < 1 means tre-mem actively re-orders / re-selects vs baseline (Jaccard = 1 would mean rerank is a no-op).

> **Single-branch dataset note:** `tre-mem` currently only has observations on `main` (1 distinct branch). The precision metric here therefore reduces to "fraction of top-K that are branch-tagged"; once a second branch is backfilled, the same harness will additionally measure cross-branch precision (off-branch tags will count as misses).

_Reproduce: `pnpm build && node scripts/benchmark.mjs` (env: `TRE_BENCH_PROJECT`, `TRE_BENCH_BRANCH`, `TRE_BENCH_K`)._
